# macOS
.DS_Store

# Original design reference — kept locally, not shipped to the repo
Personal website (1)/
Personal website (1).zip

# Local-only resume source dropped at repo root; the shipped copy is assets/files/resume.pdf
/*.pdf
